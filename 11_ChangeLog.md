# 11. Change Log

확정된 스펙이 나중에 바뀐 결정만 기록한다. 무엇을 무엇으로 바꿨는지와 **왜**
바꿨는지를 남겨, 나중에 같은 논의를 반복하거나 이유 없이 되돌리지 않게 한다.

버전별 구현 내용은 [[10_ReleaseNote]], 남은 작업은 [[09_Todo]]를 본다.

> **재구성 문서 (2026-08-01)**: 원본이 코드와 함께 유실됐다. 다른 문서에 남은
> 참조(02·03·05·08·09·10)와 AGENTS.md를 근거로 다시 작성했다. 날짜와 결정
> 내용은 그 참조들에서 확인된 것이고, 참조에 남지 않은 논의 세부는 복원할 수
> 없어 빠져 있다.

---

## 2026-08-02

### 아이콘 원본은 저장소에 넣지 않는다

- **대상**: 직접 만든 일러스트 아이콘 27종의 1024px 원본 (개당 약 1.35MB, 합계 36.4MB)
- **결정**: 원본은 `assets/icon-src/`에 두고 `.gitignore`에 넣는다. 저장소에는 `tools/make-icons.js`가 만든 128px 생성본(합계 409KB)만 커밋한다.
- **이유**: 저장소가 공개라 36MB를 이력에 남기고 싶지 않다. 한 번 들어가면 빼기도 번거롭다. 앱이 실제로 쓰는 것은 생성본뿐이다.
- **대가**: 새 컴퓨터에서는 아이콘을 다시 생성할 수 없다. 원본을 따로 옮겨야 한다 — `AGENTS.md` "저장소에 없는 것" 참고.

### 아이콘은 틴트하지 않고 상태별 파일을 둔다

- **이전**: `@expo/vector-icons` 글리프에 `color` prop으로 색을 지정
- **이후**: 일러스트는 색이 그림에 들어 있어 `color`가 통하지 않는다. 상태에 따라 색이 변해야 하는 하단 메뉴만 활성·비활성 두 벌을 두고 이름으로 고른다(`nav-home-active` / `nav-home-inactive`).
- 아이콘 세트에 없는 UI 크롬(닫기·화살표·스텝퍼)은 글리프를 그대로 쓴다. `Icon`은 이미지 우선, 없으면 글리프로 떨어진다.
- 영향: `src/Icon.js`, `src/components/BottomNav.js`

---

## 2026-08-01

### Phase 2(백엔드)를 화면 구현보다 앞으로

- **이전**: 로컬 저장소로 화면을 모두 만든 뒤 마지막에 서버로 교체 (7/30에 실제로 그렇게 했다)
- **이후**: Supabase 연동을 먼저 붙이고 그 위에 화면을 만든다
- **이유**: 코드 유실로 화면을 다시 만들게 됐는데, 서버(스키마·RLS·Storage·OAuth 설정)는 이미 준비돼 있다. 로컬 기준으로 다시 만들면 교체 작업을 한 번 더 하게 된다.
- 영향: [[09_Todo]], [[10_ReleaseNote]] v0.5

### 원격 저장소 필수

- **이전**: 로컬 git 없이 작업
- **이후**: git 저장소 + GitHub 원격(`GoKyoungEun/petApp`), Phase 단위 브랜치
- **이유**: 7/24~7/30 작업분 코드가 유실됐다. 로컬에만 있었고 복구 경로가 없었다.
- 영향: [[09_Todo]] "코드 유실", [[10_ReleaseNote]] v0.5

### 문서의 구현 상태 표기 분리

- **이전**: 기획 문서가 구현 완료 여부를 단정 (예: "현재: Supabase(PostgREST)")
- **이후**: 확정된 설계와 현재 구현 상태를 구분해서 적는다 ([[08_TechStack]]의 **[현재]** 표시)
- **이유**: 유실로 문서와 저장소가 어긋났고, 문서만 보면 없는 기능을 있다고 오해한다.

---

## 2026-07-30

### repository 계층을 Supabase로 교체

- **이전**: AsyncStorage 백킹
- **이후**: Supabase(PostgREST). 화면 코드는 한 줄도 바뀌지 않았다 — repository 인터페이스를 async로 잡아둔 설계가 의도대로 동작했다.
- 함께 정한 것
  - 사진은 base64를 jsonb에 넣지 않고 Storage에 올리고 **경로만** DB에 저장, 조회 시 서명 URL(24시간)로 바꾼다. 기록당 6장 × 수백 KB면 "펫 전체 기록" 조회(통계·몸무게)가 매번 수 MB를 끌어온다.
  - 조회에 `user_id` 조건을 넣지 않는다. 소유권은 RLS가 정한다.
  - id는 Postgres `gen_random_uuid()`. 앱이 만들던 `r1`/`p1`/`s1` 방식은 버린다.
- 영향: [[08_TechStack]], [[03_DB_Design]]

### 카카오 로그인 추가

- 구글에 이어 카카오 연동. 우려했던 이메일 동의항목(비즈앱 전환) 문제는 발생하지 않았다(scope `account_email` 통과).
- 애플은 iOS 앱스토어 배포 시점으로 미룬다.
- 영향: [[08_TechStack]]

---

## 2026-07-29

### 로그인 필수 (게스트 진입 없음)

- **이전**: 로그인 없이도 로컬에 기록할 수 있는 상태
- **이후**: 세션이 없으면 앱 본체를 마운트하지 않는다
- **이유**: 데이터 경로를 서버 하나로 유지한다. 로컬과 서버 두 벌을 동기화하는 비용이 MVP 범위에 맞지 않는다.
- 영향: [[08_TechStack]], [[06_UserFlow]] 첫 진입

### 기존 로컬 데이터를 서버로 이전하지 않음

- **이후**: 마이그레이션 없이 초기화한다. 로컬 시드(코코·보리)도 제거하고, 펫이 0마리면 등록 폼이 자동으로 열린다.
- **이유**: 기존 로컬 데이터는 개발 중 테스트 데이터다. 마이그레이션을 만들 가치가 없다.
- 영향: [[08_TechStack]]

### 백엔드를 Supabase로 확정

- **이유**: 관계형/이벤트 스키마 적합성, Auth(카카오·구글·애플 OAuth), Storage, 무료 티어.
- 영향: [[08_TechStack]], [[03_DB_Design]]

### 기록당 사진 최대 5장 → 6장

- **이유**: 전체 기록보기에서 사진을 카드 안 3열 격자로 표시하기로 했다. 6장이면 3열 2행으로 딱 떨어지고, 5장은 마지막 줄이 비어 어색하다.
- 영향: [[02_MVP_Requirement]] §6, [[03_DB_Design]] "사진 제한", [[05_UI_UX]], [[09_Todo]] 기술 검증 항목

### 건강사진 탭에 "전체" 필터 칩 추가

- **이전**: 눈·코·발·피부 및 모질·기타 5개 분류만
- **이후**: 앞에 "전체" 칩을 기본 선택으로 둔다
- **이유**: 분류를 고르지 않으면 아무것도 안 보이는 상태가 기본이 되면, 사진을 훑어보려는 사용자가 매번 탭을 눌러야 한다.
- 영향: [[02_MVP_Requirement]] §6

### 데이터 계층을 TanStack Query로 통일

- **이전**: 화면마다 `useEffect` + `useState`, 갱신은 `dataVersion` 수동 무효화
- **이후**: 기록·펫·일정 세 도메인 모두 Query 캐시. 쓰기 후 키 prefix를 invalidate.
- **이유**: 기록을 저장해도 캘린더 표시나 전체 기록 목록이 갱신되지 않았다. 화면마다 갱신을 챙기는 방식은 화면이 늘수록 빠뜨린다.
- 영향: [[08_TechStack]]

---

## 2026-07-24

### "오늘도 평소와 같아요"를 어제 기록 복사 방식으로 확정

- **이전**: 기본 기록을 고정값으로 일괄 생성
- **이후**: **어제 기록**을 기준으로 종별 핵심 항목만 오늘로 복사한다. 어제 기록이 없으면 아무것도 만들지 않고 "어제 기록이 없어 불러올 수 없어요" 토스트를 띄운다. 사진·메모는 복사하지 않는다.
- **이유**: 고정값은 그 반려동물의 평소가 아니다. 어제 기록이 실제 "평소"에 가깝다.
- 영향: [[02_MVP_Requirement]] §3, [[06_UserFlow]]

### 빠른 기록의 사진·메모 항목을 2스텝으로

- **이전**: 선택 즉시 저장, 저장 버튼 없음
- **이후**: 사진·메모를 붙일 수 있는 항목(식사·배변·소변·구토)은 2스텝. 1스텝에서 상태를 고르고, 2스텝에서 사진/메모를 더하거나 [건너뛰기]로 상태만 저장한다.
- **이유**: 즉시 저장은 빠르지만 사진을 붙일 자리가 없고, 잘못 눌렀을 때 되돌릴 여지가 없다. 건너뛰기를 두어 빠른 경로는 유지했다.
- 영향: [[02_MVP_Requirement]] §4, [[05_UI_UX]]

### Expo SDK 57 → 54

- **이유**: 공개 Expo Go 앱이 SDK 54까지만 지원한다. 상위 SDK는 개발 빌드가 필요해 배포·테스트가 번거롭다.
- 영향: `AGENTS.md`, `package.json` 버전 핀
