# 09. Todo

## 코드 유실 (2026-08-01)

7/24 ~ 7/30 작업분의 **코드가 유실됐다.** 원격에 올려두지 않은 상태에서
로컬이 7/23 스냅샷으로 되돌아갔고, 복구 경로(에디터 로컬 히스토리, 번들
캐시, 휴지통, 섀도우 복사본, 대화 기록)를 모두 확인했으나 남아 있지 않았다.

- **기획 문서는 그대로다.** 아래 "완료" 목록은 설계가 확정됐다는 뜻으로는
  여전히 유효하고, 재구현할 때 그대로 쓴다.
- **서버 자원도 그대로다.** Supabase 프로젝트·스키마·Storage 버킷·RLS와
  카카오/구글 OAuth 콘솔 설정은 클라우드에 살아 있다. 코드만 다시 쓰면 된다.
- **앱 아이콘·스플래시도 유실됐다** (2026-08-03 확인). `assets/`에 파일이
  6개 있어 남아 있는 줄 알았으나 전부 Expo 기본 템플릿이었다 — `icon.png`과
  `android-icon-foreground.png`은 파란 "Λ" 로고, `splash-icon.png`은 회색
  격자 플레이스홀더. 생성 스크립트 `tools/make-app-icons.js`도 없었다.
  2026-08-03에 다시 만들었다([[10_ReleaseNote]] v0.5).

재발 방지로 git 저장소를 만들고 원격(GitHub)에 연결했다. 이후 작업은
Phase 브랜치에서 진행한다.

## 현재 저장소에 있는 것 (2026-08-03)

- **Supabase 연동** — 로그인 게이트(구글·카카오, `src/screens/LoginScreen.js`),
  클라이언트 `src/supabase.js`, 인증 `src/auth.js`, 컬럼 매퍼 `src/db.js`,
  사진 저장 `src/photoStore.js`, 스키마 사본 `supabase/schema.sql`.
  기록·펫 repo 2종이 Supabase 호출로 바뀌었고 화면 코드는 그대로다.
  쓰기 실패는 시트 안에 표시(`src/components/SheetError.js`)
- **마이페이지** — 계정 카드(프로필 사진·이름·이메일 + 로그아웃), 반려동물
  관리(목록·전환·수정·추가, PetForm 재사용, 최대 5마리), 설정(푸시 알림 준비 중
  표기), 앱 정보(버전) (`src/screens/MyScreen.js`)
- **통계** — 기간 필터 7·30일/3개월/1년, 카드 6종(기록한 날짜 수·컨디션·배변·
  소변·산책·몸무게), 몸무게 별도 필터(1·3·6개월/1년/전체)와 선 그래프,
  데이터 부족 안내 (`src/stats.js`·`StatsScreen`·`Sparkline`)
- **몸무게 전용 화면** — 통계 몸무게 카드 "자세히"로 진입. 눈금·kg 라벨·날짜
  비례 x축 선 그래프, 기간 필터 1·3·6개월/1년/전체, 날짜별 측정 로그(회차별
  증감, 눌러서 수정), 화면 내 "+ 기록" (`src/screens/WeightScreen.js`)
- **건강 일정** — 일정 탭(등록·수정·삭제·완료, D-day, 예정·지난 구분),
  반복 주기 완료 시 다음 일정 자동 생성, 완료 직후 4초 실행취소(상태 복원 +
  자동 생성분 삭제), 캘린더 일정 배지·날짜 패널, 홈 "다음 일정" 실데이터
  (`src/scheduleRepo.js`·`src/queries/schedules.js`·`ScheduleScreen`·`ScheduleForm`)
- 기록 날짜 선택 — 시트마다 "기록 날짜"(기본 오늘, 월 달력, 미래 차단),
  캘린더에서 날짜 고르고 "기록 추가" (`src/components/DateField.js`)
- 홈 + 빠른 기록(2스텝 사진·메모 첨부), 종별 분기, 실행취소 스낵바
- 오늘도 평소와 같아요(어제 기록 복사 + 없으면 토스트)
- repository 계층 + AsyncStorage 영속화
- 반려동물 등록·전환·수정·삭제(사진 포함, 최대 5마리)
- 전체 기록보기(항목 탭·날짜별 카드), 캘린더(월간)
- 실제 날짜 적용 (`src/date.js` — 로컬 캘린더 기준 YYYY-MM-DD)
- 반응형 스케일링 (`src/scale.js` — 기기 폭 기준), iOS 키보드 회피
- 기록·펫 도메인 TanStack Query (`src/queries/records.js`·`pets.js`)
- 기록 수정·삭제 시트 (`src/components/EditRecordSheet.js`)
- 직접 만든 일러스트 아이콘 27종 (`assets/icon`, `tools/make-icons.js`로 생성)
- 앱 아이콘·스플래시 하트+발바닥 마크 6종 (`tools/make-app-icons.js`로 생성),
  `expo-splash-screen` 연결 (2026-08-03)
- 건강사진 (`recordType: healthPhoto`): 더보기 → 전용 시트(카메라·갤러리
  멀티선택, 기록당 최대 6장, 저장 전 압축 1280px·JPEG q0.7, 카테고리 필수,
  메모 선택), 전체기록 탭 연결('전체'+카테고리 필터), 카드 안 3열 격자,
  홈·캘린더 "N장" 표시, 수정 시트에서 사진·분류 편집

## 재구현이 필요한 것

**없다 (2026-08-03).** 유실분을 모두 되찾았다. 아래 "유실 전 구현 현황"의
완료 목록이 전부 저장소에 있고, 그 위에 유실 전에 없던 것(기록 날짜 선택,
생년월일 룰렛, 캘린더 월↔주 버튼 방식)이 더해졌다.

앞으로 할 일은 재구현이 아니라 새 기능이다 — 아래 "다음 구현 우선순위" 3번부터.

### 실기기 검증 (2026-08-03 — 통과)

Expo Go(터널)로 한 바퀴 돌려 전부 정상 동작을 확인했다.

1. 구글·카카오 로그인 → 세션 유지 → 로그아웃 → 재로그인. **터널 필수** —
   LAN 모드는 리디렉트가 IP 주소라 GoTrue가 거부한다(08_TechStack "백엔드")
2. 펫 등록(사진·생년월일 룰렛) → 기록 저장·수정·삭제 → 실행취소 →
   지난 날짜로 저장 → 캘린더·전체기록 반영
3. 건강사진 업로드 → 서명 URL로 표시 → 수정 시트에서 추가·삭제
4. `supabase/schema.sql`과 서버 스키마 대조 — 테이블 5종과 매퍼가 쓰는 모든
   컬럼이 일치. `/rest/v1/<table>?select=<컬럼들>`이 200이면 통과, 없으면
   42703이 뜬다. CHECK 제약은 이 방법으로 볼 수 없어 5번으로 확인했다
5. 일정 저장이 CHECK 제약에 걸리지 않는지 — **반복을 켠 일정이 저장되고 완료 시
   다음 일정이 생기는 것까지 확인**. `repeat_interval_type`(day·week·month·year)은
   근거 문서 없이 `schema.sql`을 다시 쓰며 정한 값이었는데, 서버 제약과 맞았다
6. 캘린더 월↔주, 일정 배지, 통계 카드 6종, 몸무게 화면(그래프·측정 로그)

중첩 스크롤(룰렛·시트 안 달력)이 폼 스크롤과 충돌하지 않는 것도 이때 확인됐다.

### schema.sql이 서버와 다른 곳 (2026-08-03)

`schedules.linked_record_id`의 외래키. `schema.sql`은 `medical_records`를
가리키게 썼지만 **서버는 그렇지 않다** — 방금 만든 완료 기록의 id를 넣었더니
`schedules_linked_record_id_fkey` 위반으로 거부했다. 어느 테이블을 가리키는지는
익명 키로 볼 수 없어 확인하지 못했다(`health_records`일 가능성이 높다.
03_DB_Design의 HealthRecord recordType 목록에 `medicalRecord`가 있다).

앱은 이 컬럼을 쓰지 않는다. "일정 완료 시" 3단계의 "scheduleId로 연결"은
`medical_records.schedule_id`이고 이건 반대 방향 포인터라 없어도 연결이
성립한다. 그래서 동작에는 영향이 없다. **빈 프로젝트에 `schema.sql`을 실행할
계획이 생기면 그때 서버 정의를 확인해 맞춘다.**

`medical_records.medical_type`의 CHECK 제약은 걱정거리였는데 통과했다 —
완료 처리가 실제로 저장되는 것을 확인했다(직접 입력 일정의 이름이 그대로
들어가야 해서 `schema.sql`에는 제약을 두지 않았다).

## 유실 전 구현 현황 (2026-07-29 기준 — 설계 근거로 보존)

완료

- 홈 + 빠른 기록(2스텝 사진·메모 첨부)
- 종별 분기(강아지 산책 / 고양이 컨디션)
- 오늘도 평소와 같아요(어제 기록 복사 + 없으면 토스트)
- repository 계층 + AsyncStorage 영속화
- 반려동물 등록·전환·수정·삭제(사진 포함, 최대 5마리)
- 전체 기록보기(항목 탭·날짜별 카드)
- 캘린더(월간·오늘/기록 표시·날짜별 기록)
- 통계 화면(기간필터 7·30일/3개월·1년, 카드 6종, 몸무게 별도 필터+스파크라인, 데이터 부족 안내)
- 실제 날짜 적용, 반응형 스케일링, iOS 키보드 회피
- 캘린더 월↔주 축소(스크롤 연동 애니메이션)
- 건강 일정: 일정 탭(등록/수정/삭제/완료, D-day, 예정·지난 구분), 반복 주기 완료 시 다음 일정 자동 생성, 완료 직후 4초 스낵바 실행취소(상태 복원+자동 생성 일정 삭제), 캘린더 일정 배지+날짜 패널 표시, 홈 "다음 일정" 실데이터화. **일정 도메인은 TanStack Query 적용**(`src/queries/schedules.js`)
- 몸무게 전용 화면(`src/screens/WeightScreen.js`, 통계 몸무게 카드 "자세히"로 진입): react-native-svg 선 그래프(격자·kg 라벨·날짜 비례 x축), 기간 필터 1·3·6개월/1년/전체, 날짜별 측정 로그+회차별 증감, 화면 내 "+ 기록" 버튼, 데이터 부족 안내(02 §7)
- 마이페이지(`src/screens/MyScreen.js`): 계정 카드(로그인한 이름·이메일·프로필 사진 + 로그아웃), 반려동물 관리(목록·전환·수정·추가, PetForm 재사용), 설정(푸시 알림 준비 중 표기), 앱 정보(버전)
- 건강사진(recordType `healthPhoto`): 더보기 → 전용 기록 시트(카메라·갤러리 멀티선택, 기록당 최대 6장, 저장 전 자동 압축 1280px·JPEG q0.7, 카테고리 눈·코·발·피부및모질·기타 필수, 메모 선택). 전체기록 건강사진 탭 데이터 연결('전체'+카테고리 필터), 사진은 카드 안 3열 격자(최대 3열 2행), 홈 오늘 기록 "N장"·캘린더 날짜 패널 표시, 수정 시트(사진 추가·삭제, 카테고리·메모·날짜·시간 — 사진 첨부 가능한 식사·배변·소변·구토 기록의 수정 시트에서도 동일하게 사진 표시·편집). 빈 항목 탭 공통 "기록 추가" 버튼(02 §6). 카메라·갤러리는 실기기(Expo Go) 검증 필요

- 기록·펫 도메인 TanStack Query 전환 — 세 도메인 모두 Query 캐시로 통일(`src/queries/records.js`·`pets.js`·`schedules.js`), dataVersion 방식 제거. 백엔드 교체 시 queryFn/repo만 바꾸면 됨
- Supabase 프로젝트 생성 + 스키마 실행 완료 (`supabase/schema.sql` — 03 기반 테이블·인덱스·RLS·Storage 버킷)
- supabase-js 클라이언트(`src/supabase.js`) + 인증 계층(`src/auth.js`) + 로그인 화면(`src/screens/LoginScreen.js`) + 인증 게이트(`App.js`). MY 화면 계정 카드 = 로그인 계정 표시 + 로그아웃. **로그인 필수 방식 확정, 로컬 데이터 마이그레이션 안 함** (11_ChangeLog)
- **구글 로그인 동작 확인 — 웹 + 실기기(Expo Go) 둘 다.** 로그인 → 세션 유지 → MY 계정 표시 → 로그아웃
- 앱 아이콘·스플래시(하트+발바닥 마크) — Expo 기본 아이콘 교체, iOS/안드로이드 적응형·모노크롬·favicon 일괄 생성(`tools/make-app-icons.js`), `expo-splash-screen` 연결. 로그인 화면 로고도 실제 아이콘으로 교체

- **repository 계층 Supabase 교체 완료** (2026-07-30): 기록·펫·일정 repo 3종이 Supabase 호출로 바뀌었고 화면 코드는 그대로. snake_case↔camelCase 매핑(`src/db.js`), `user_id` 주입, 사진 base64 → Storage 업로드(`src/photoStore.js`, DB엔 경로만·조회 시 서명 URL), 로컬 시드(코코·보리) 제거, 펫 0마리면 등록 폼 자동 표시, 쓰기 실패를 시트 안에 표시. **실제 로그인 세션이 필요한 CRUD 왕복·사진 업로드는 미검증**

다음 구현 우선순위 (백엔드 = Supabase, 카카오·구글 로그인 먼저)

1. ~~repository 교체 실사용 검증~~ **통과 (2026-07-30, 실기기)** — 펫 등록 → 기록 저장/수정/삭제 → 통계·캘린더 → 일정 → 사진 업로드·표시까지 한 바퀴 정상. Storage 정책도 이때 함께 확인됨(사진 업로드 성공)
2. ~~카카오 로그인~~ **완료 (2026-07-30)** — Kakao Developers 앱 생성 + Supabase Providers 설정, 실기기 로그인 확인. 걱정했던 이메일 동의항목(비즈앱 전환) 문제는 발생하지 않았다(scope `account_email` 통과). 애플 로그인만 남음(iOS 배포 시)
3. ~~일정 완료 시 실제 완료 기록(MedicalRecord) 생성·연결~~ **완료 (2026-08-03)**
   — 03_DB_Design "일정 완료 시" 네 단계를 모두 한다. 완료를 누르면 시행일·
   병원·제품을 확인받는 시트가 뜨고, 확인하면 `medical_records` 행을 만들어
   `linked_record_id`로 잇는다. 실행취소는 상태·완료 기록·자동 생성된 다음
   일정 셋을 모두 되돌린다
4. ~~기록 먼저 생성 시 "다음 일정도 등록하시겠어요" 안내 흐름~~ **완료
   (2026-08-03)** — 더보기 → 병원 기록으로 일정 없이 완료 기록을 남기고,
   저장 후 안내가 뜬다. "일정 등록"을 고르면 종류·병원·제품이 채워진 일정 폼이
   열린다 (06_UserFlow "기록 먼저 생성")
5. 일정 푸시 알림 (notificationSetting은 저장만 하고 예약은 미구현)
6. 회원 탈퇴·데이터 삭제 흐름, 이용약관·개인정보 처리방침 문서 (로그인 화면 하단 문구는 있으나 링크 없음)
7. 기록 목록 페이지네이션 — 지금은 한 펫당 `limit(5000)`에서 잘린다. 사진 썸네일(`record_photos.thumbnail_url`)도 미생성이라 목록에서 원본을 받는다

## 완료된 기획

- 서비스 단계별 로드맵
- MVP 목표
- 로그인 방식
- 반려동물 등록 정보
- 홈 핵심 항목
- 오늘도 평소와 같아요
- 빠른 기록 방식
- 배변, 소변, 구토 데이터
- 컨디션 상태와 증상
- 건강사진 분류
- 캘린더 구조
- 몸무게 그래프
- 일정과 기록 분리
- 건강 일정 종류
- 일정 알림
- 핵심 통계 항목
- 통계 기간 필터

## 다음 기획 우선순위

1. 통계 화면 상세 설계
2. 마이페이지 및 설정 (MVP 구현됨 — 계정·데이터 삭제 섹션 설계만 남음, 7·8번과 함께)
3. 푸시 알림 권한 및 예외 흐름
4. 반려동물 전환 UX
5. 기록 생성 및 수정 화면 상세
6. 온보딩
7. 데이터 삭제 및 회원 탈퇴
8. 개인정보 처리 범위
9. MVP 화면 목록 확정
10. DB 최종 설계

## 통계 화면에서 추가로 정할 내용

구현하며 정해진 것 (2026-08-03 — `src/stats.js`·`StatsScreen`)

- **카드 순서** — 05_UI_UX에 이미 확정돼 있었다. 그대로 따랐다
- **그래프 유형** — 몸무게는 선 그래프(02_MVP §7), 비율은 가로 막대. 막대에서
  0%는 아예 그리지 않는다 — 얇게라도 보이면 "조금 있음"으로 읽힌다
- **배변 상태 분류** — 빠른 기록 시트의 세 갈래(정상·설사·색 이상)를 그대로
  쓴다. 통계에만 다른 분류를 두면 기록할 때와 볼 때가 어긋난다
- **소변 통계 표현 방식** — 횟수와 하루 평균만. 빠른 기록도 "지금 다녀왔어요"
  하나뿐이라 나눌 상태가 없다
- **기록일 계산 기준** — 유형과 무관하게 하루에 한 건이라도 있으면 1일.
  무엇을 적었는지가 아니라 "적었는가"를 보는 카드다
- **데이터 부족 상태** — 카드마다 해당 기록이 0건이면 안내 문구. 몸무게는
  측정이 2번 미만이면 수치만 내고 그래프를 그리지 않는다(05_UI_UX)
- **산책 평균의 분모** — 기간이 아니라 산책 횟수. 안 나간 날까지 나누면
  "한 번에 얼마나 걷나"를 알 수 없다

아직 안 정한 것

- 산책 시간 입력 단위 (지금은 5분 단위 스테퍼)
- 기간 비교 기능 포함 여부 (지난 기간 대비 증감)

## 기술 검증 항목

- 소셜 로그인 계정 연결
- 이미지 압축 후 AI 활용 가능 품질
- 최대 6장 업로드 성능
- 푸시 알림 예약 방식
- 일정 반복 생성 로직
- 다중 반려동물 데이터 전환
