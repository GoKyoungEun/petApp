# 10. Release Note

## v0.1 기획 초안

확정된 내용

- 반려동물 건강 다이어리 앱의 단계별 로드맵 정의
- MVP는 무료 일상 건강기록 앱으로 범위 제한
- 소셜 로그인만 지원
- 반려동물 최대 5마리 등록
- 종별 홈 빠른 기록 구성
- 오늘도 평소와 같아요 기능
- 이벤트 기반 기록 구조
- 건강사진 별도 관리
- 월간 캘린더와 날짜별 기록 확인
- 건강 일정과 실제 완료 기록 분리
- 몸무게 그래프
- 핵심 통계 범위 확정

## v0.2 통계 범위 확정

통계 항목

- 몸무게 변화
- 배변 상태와 횟수
- 소변 횟수
- 산책 횟수와 총시간
- 컨디션 변화
- 기록한 날짜 수

기간 필터

일상 통계

- 7일
- 30일
- 3개월
- 1년

몸무게 통계

- 1개월
- 3개월
- 6개월
- 1년
- 전체

## v0.3 구현 1차 (2026-07-24)

claude.ai/design 프로토타입 기반 React Native + Expo(SDK 54) 앱으로 구현 시작.

구현

- 홈 + 빠른 기록(2스텝 사진·메모 첨부), 종별 분기, 실행취소 스낵바
- 오늘도 평소와 같아요: 어제 기록 복사 방식으로 확정
- repository 계층 + AsyncStorage 로컬 영속화 (백엔드 교체 가능 구조)
- 반려동물 등록·전환·수정·삭제(사진 포함, 최대 5마리)
- 전체 기록보기, 캘린더(월간)
- 실제 날짜 적용, 반응형 스케일링(기기 폭 기준), iOS 키보드 회피

스펙 변경 결정은 [[11_ChangeLog]] 참고. 미구현: 소셜 로그인, 백엔드, 통계, 건강 일정, 몸무게 그래프, 푸시 알림.

## v0.4 구현 2차 (2026-07-28 ~ 07-29)

구현

- 통계 화면(기간 필터, 카드 6종) + 몸무게 전용 화면(SVG 선 그래프)
- 건강 일정 탭(등록·수정·삭제·완료·실행취소, D-day, 반복 자동 생성) — TanStack Query 도입
- 캘린더 월↔주 축소, 일정 배지·날짜별 일정 표시
- 마이페이지(반려동물 관리·설정·앱 정보)
- 사용자 제작 일러스트 아이콘 적용
- 건강사진 기록(카메라·갤러리, 자동 압축 1280px·JPEG q0.7, 카테고리 분류, 기록당 최대 6장) + 전체기록 탭 연결·3열 격자 표시
- 데이터 계층 정리: 기록·펫·일정 세 도메인 모두 TanStack Query로 통일(dataVersion 수동 무효화 제거)

이로써 하단 5개 탭이 모두 실제 화면으로 동작.

백엔드 준비

- 백엔드를 **Supabase**로 확정, 스키마 SQL 작성 완료(`supabase/schema.sql` — 테이블 5종·RLS·Storage 버킷)
- 소셜 로그인은 카카오·구글 먼저, 애플은 추후

미구현: 소셜 로그인·서버 연동(작업 예정), 푸시 알림, MedicalRecord 연동.

## v0.5 코드 유실과 재구축 (2026-08-01)

v0.3~v0.4(7/24~7/30) 작업분의 **코드가 유실됐다.** 원격 저장소 없이 로컬에만
있었고, 로컬이 7/23 스냅샷으로 되돌아갔다. 복구 경로를 모두 확인했으나 남아
있지 않았다. 기획 문서와 서버 자원(Supabase, OAuth 콘솔), 앱 아이콘은 무사하다.

재발 방지

- git 저장소 생성, GitHub 원격(`GoKyoungEun/petApp`) 연결
- 이후 작업은 Phase 브랜치에서 진행

재구축 Phase 1 (기반)

- 실제 날짜 적용 — `src/date.js`. 로컬 캘린더 기준 YYYY-MM-DD로 통일하고,
  `new Date('YYYY-MM-DD')`가 UTC 자정으로 파싱돼 하루가 밀리던 문제를 없앴다.
  앱을 켜둔 채 자정을 넘겨도 날짜가 갱신된다.
- 반응형 스케일링 — `src/scale.js`. `scaled()`가 StyleSheet 전체를 훑어 길이
  키만 변환한다. 글꼴은 절반 비율, 비율은 0.85~1.25로 제한.
- iOS 키보드 회피 — PetForm과 QuickRecordSheet에 KeyboardAvoidingView.
- 기록·펫 도메인 TanStack Query 전환 — `src/queryClient.js`, `src/queries/*`.
  쓰기 후 키 prefix 하나만 무효화하면 홈·캘린더·전체 기록이 함께 갱신된다.
- 기록 수정·삭제 시트 — `src/components/EditRecordSheet.js`. recordType별로
  필요한 필드만 띄우고, 기록 기준일도 고칠 수 있다(02 §8). 진입은 전체
  기록보기의 개별 기록. 캘린더 날짜 패널은 항목별 집계라 개별 기록을 특정할
  수 없어 해당 항목의 전체 기록으로 보낸다.
- 직접 만든 일러스트 아이콘 27종 — 기본 기록 14, 하단 메뉴 5(활성·비활성 쌍),
  일정 종류 3. `tools/make-icons.js`가 1024px 원본의 여백을 잘라 128px로
  줄인다(36.4MB → 409KB). 색이 그림에 들어 있어 틴트가 통하지 않으므로,
  상태로 색이 바뀌는 하단 메뉴만 두 벌을 두고 이름으로 고른다.
- 건강사진 — 더보기의 전용 시트(`src/components/HealthPhotoSheet.js`).
  카메라·갤러리 다중 선택, 최대 6장, 저장 전 1280px·JPEG q0.7 압축
  (`src/photo.js`), 분류 필수. 전체 기록보기 탭에 "전체" 칩과 카테고리 필터,
  카드 안 3열 격자. 홈·캘린더는 사진 수를 세어 "N장".

앱 아이콘·스플래시 (2026-08-03)

v0.4의 하트+발바닥 마크도 유실됐다는 것이 뒤늦게 확인돼(위 "재발 방지"와
[[09_Todo]]는 남아 있다고 잘못 적고 있었다) 다시 만들었다.

- 마크를 비트맵 원본이 아니라 `tools/make-app-icons.js` 안의 도형 수식으로
  기술한다. 한 모양을 여섯 파일로 뽑아야 하는데 크기(64~1024px)도, 여백도
  (iOS는 가장자리까지, 안드로이드는 적응형 안전원 안), 색 처리도(컬러 /
  안드로이드 테마 아이콘용 단색 실루엣) 제각각이라 한 장을 여섯 번 자르면
  작은 크기에서 가장자리가 뭉개지고 모노크롬은 아예 못 만든다.
- 하트는 마름모 + 위쪽 두 변에 얹은 원 두 개. 원의 반지름을 변의 절반으로
  잡으면 원이 마름모의 좌우 꼭짓점을 **아래 변에 접한 채로** 지나가서 윤곽에
  꺾임이 생기지 않는다. 다르게 나누면 어깨에 각이 보인다.
- 발바닥은 하트에서 파낸다. 발가락·발볼 사이 간격은 전부 0.09단위 이상 —
  favicon이 32px로 줄었을 때 약 1.4px로, 이보다 좁으면 서로 붙어버린다.
- 색은 `src/theme.js` 팔레트. 하트는 primary(#B85C2E)에서 accent를 35% 섞은
  색으로 위쪽만 밝히고, 파낸 자리는 peachLight(#FFEFD6)로 채워 안드로이드
  배경 레이어와 같은 색으로 만난다.
- `expo-splash-screen`(~31.0.13) 설치 + `app.json` plugins 연결. `App.js`는
  펫 목록 첫 조회가 끝날 때까지 네이티브 스플래시를 붙잡아, 켜자마자 펫 없는
  홈이 한 프레임 스치는 것을 막는다(3초 백스톱).

검증: 웹 번들 빌드 통과, 브라우저에서 홈 렌더 확인(콘솔 에러 없음),
생성물을 기기 표시 크기(16·32·48·60·96px)로 줄여 눈으로 확인. 네이티브
스플래시 자체는 개발 빌드가 있어야 보이므로 미확인 — Expo Go는 자체 스플래시를
쓴다.

남은 것: Supabase + 로그인(Phase 2), 통계·몸무게·일정·MY 화면(Phase 3).

Phase 2를 화면보다 앞에 두기로 했다. 7/30에는 로컬로 화면을 다 만든 뒤 서버로
갈아끼웠지만, 지금은 서버가 이미 준비돼 있어 먼저 붙이면 그 교체 작업을 한 번
더 하지 않아도 된다.

검증 상태: 날짜 수정만 브라우저에서 동작을 확인했다. 그 이후 개발 서버가
번들링 중에 반복해서 죽어 나머지는 정적 점검만 거쳤다. 실기기 확인이 남아 있다.

## v0.6 재구축 Phase 2 — Supabase 연동 (2026-08-03)

유실된 서버 연동 코드를 다시 썼다. 서버(프로젝트·스키마·RLS·Storage·OAuth
콘솔)는 클라우드에 그대로 있어 앱 코드만 붙이면 되는 상태였다.

- **로그인 게이트** — 세션이 없으면 앱 본체를 마운트하지 않는다(`App.js`).
  구글·카카오 버튼(`src/screens/LoginScreen.js`), PKCE + 인앱 브라우저로
  돌아온 `code`를 세션으로 교환(`src/auth.js`). 로그아웃 시 Query 캐시를 비워
  이전 계정 데이터가 남지 않게 했다.
- **클라이언트 하나** — `src/supabase.js`. 세션은 AsyncStorage(웹은
  localStorage), 백그라운드에서는 토큰 갱신 타이머를 멈춘다. 키가 없으면 어디서
  받아야 하는지 적힌 오류로 즉시 멈춘다.
- **컬럼 매핑** — `src/db.js`. 테이블당 FIELDS 한 벌에서 select 목록·row→앱·
  패치→row를 만든다. 필드를 늘릴 때 세 군데 중 하나를 빠뜨려 "조회는 되는데
  저장만 안 되는" 버그가 나던 자리를 없앴다.
- **사진** — `src/photoStore.js`. base64를 jsonb에 넣던 것을 Storage 업로드로
  바꿨다. DB에는 경로만 두고 읽을 때 24시간 서명 URL로 바꾼다. Hermes에 `atob`이
  없어 base64 디코딩을 직접 넣었다. 기록·펫을 지우거나 사진을 빼면 Storage
  파일도 함께 지운다(DB만 cascade라 안 지우면 파일이 남는다).
- **repo 2종 교체** — `repository.js`·`petRepo.js`가 Supabase를 부른다. 화면
  코드는 그대로. 인메모리 캐시, 로컬 시드(코코·보리), `r1`/`p1` id 생성이
  사라졌다(id는 `gen_random_uuid()`). 로컬에 남는 것은 "선택된 펫"뿐이고 키에
  user_id가 붙는다. 펫이 0마리면 등록 폼이 자동으로 열린다.
- **쓰기 실패 표시** — `src/components/SheetError.js`. 스낵바가 Modal 위로 못
  올라와서, 시트가 열린 채 실패하면 저장 버튼 위에 이유를 띄운다. 그 전에는
  저장이 실패해도 버튼이 안 먹는 것처럼만 보였다.
- `supabase/schema.sql` 재작성(03_DB_Design 기준), `app.json`에 `scheme: petapp`.

검증 상태: 작성 시점에는 `.env`가 없어 서버에 붙여 보지 못했다. 이후 키를 받아
**실기기(Expo Go)로 로그인부터 사진 업로드까지 확인했다** — 09_Todo "실기기 검증".

미구현: 통계·몸무게·일정·MY 화면(Phase 3), 푸시 알림, MedicalRecord 연동.

## v0.7 건강 일정 (2026-08-03)

일정 도메인을 다시 만들었다. 테이블(`schedules`)과 RLS는 서버에 이미 있어
앱 코드만 붙였다.

- **일정 탭** — 예정·지난 구분, D-day 배지, 완료 처리. 예정인데 날짜가 지난
  일정은 지난 쪽으로 내리지 않고 위에 남기고 `D+n`으로 표시한다. 아직 해야 할
  일이라 눈에 보여야 한다
- **등록·수정 시트** — 종류 6종(직접 입력은 이름 필수), 예정일, 병원·제품·메모,
  반복 주기, 알림. 06_UserFlow "건강 일정 등록" 순서 그대로
- **반복** — 완료하면 다음 일정이 자동으로 생긴다. 월·년 단위는 말일로 당긴다:
  "매월 31일" 심장사상충은 2월 28일에 오지, 3월 3일로 넘어가지 않는다
- **완료 실행취소** — 4초 스낵바. 상태를 되돌리고 자동 생성된 다음 일정도
  함께 지운다. 하나만 하면 어긋난 상태가 남는다
- **캘린더** — 날짜 칸에 일정 배지(기록 점과 색이 다르다), 날짜 패널에 그날
  일정을 기록보다 위에 낸다. 날짜를 열어 보는 이유가 대개 "이날 뭐 해야 하지"라서
- **홈** — 하드코딩돼 있던 "심장사상충 D-3"을 실제 데이터로 바꿨다. 다가오는
  일정이 없으면 카드를 아예 내지 않는다

스낵바의 "실행취소"가 무엇을 되돌릴지를 띄우는 쪽이 정하게 바꿨다. 기록 저장은
방금 만든 행을 지우는 것이고 일정 완료는 상태 복원 + 자동 생성분 삭제라,
동작을 하나로 고정해 둘 수 없다.

알림은 설정만 저장하고 실제 푸시는 가지 않는다. 시트에 "준비 중"이라고 적어
뒀다 — 없는 기능을 있는 것처럼 두면 알림을 기다리다 일정을 놓친다.

일정 완료 시 MedicalRecord 생성·연결은 넣지 않았다(09_Todo 우선순위 3).

검증: 반복 주기와 D-day 계산 15케이스 통과(윤년 2/29 +1년 → 2/28, 1/31 +1개월
→ 2/28, 해 넘김 포함). **실기기 확인 완료** — 반복을 켠 일정 저장, 완료 시 다음
일정 자동 생성, 실행취소까지. `repeat_interval_type` 값이 서버 CHECK 제약과
맞는지가 유일한 미확인 지점이었는데 통과했다.

## v0.8 통계 (2026-08-03)

05_UI_UX "통계 화면"의 카드 순서와 기간 필터를 그대로 구현했다. 이로써 하단
5개 탭이 모두 실제 화면이 되고 `StubScreen`은 지웠다.

- 기간 필터 7일·30일·3개월·1년, 카드 6종: 기록한 날짜 수 → 컨디션 → 배변 →
  소변 → 산책 → 몸무게
- 몸무게 카드만 자기 필터를 갖는다(1·3·6개월·1년·전체). 체중은 며칠 단위로
  보면 의미가 없고, 나머지는 최근 흐름을 보는 카드라 기간이 짧다
- 몸무게 선 그래프(`Sparkline`, react-native-svg). **x축이 순번이 아니라
  날짜에 비례한다** — 3일 간격 두 번과 3개월 간격 두 번이 같은 모양이면 추이를
  잘못 읽는다
- 데이터가 부족하면 그래프를 억지로 만들지 않는다: 기록 0건이면 카드마다 안내
  문구, 몸무게는 측정이 2번 미만이면 수치만 낸다

집계는 화면에서 떼어 `src/stats.js`에 순수 함수로 뒀다. 평균의 분모나 빈 구간
처리는 눈으로 봐서 맞는지 알기 어렵고, 화면 안에 있으면 확인하려면 로그인부터
해야 한다.

카드 다섯 장은 기간 조회 한 번(`listByRange`)으로 만든다. 이 조회는 사진
서명 URL을 만들지 않는다 — 통계는 사진을 쓰지 않는데 1년치면 수백 장에
서명하느라 왕복만 늘어난다.

09_Todo "통계 화면에서 추가로 정할 내용"에 남아 있던 항목 대부분이 이때
정해졌다(기록일 계산 기준, 배변 상태 분류, 소변 표현, 데이터 부족 상태,
산책 평균의 분모). 산책 시간 입력 단위와 기간 비교 기능은 아직 미정.

검증: 집계 함수 19케이스 통과 — 같은 날 여러 건, 0건일 때 0으로 나누지 않기,
입력 정렬 순서와 무관한 "최근" 판정, 몸무게 1점이면 변화 null. 선 그래프는
좌표를 직접 재서 날짜 비례(1일:9일 간격 = 18.8px:169.2px)와 몸무게가 같을 때
NaN이 나오지 않는 것을 확인했다. 화면 동작도 실기기에서 확인했다.

## v0.9 몸무게 전용 화면 (2026-08-03)

통계의 몸무게 카드에서 "자세히"로 들어간다. 카드의 스파크라인은 추이만 보여
주는 작은 그림이라, 눈금과 실제 측정값을 보려면 따로 필요했다(02_MVP §7).

- 눈금 5줄·kg 라벨·양끝 날짜가 붙은 선 그래프. x축은 여기서도 날짜에 비례한다
- 기간 필터 1·3·6개월·1년·전체
- 날짜별 측정 로그. 각 줄에 **바로 이전 측정 대비 증감**을 함께 낸다 — 한 번에
  얼마나 늘었는지는 절대값만 봐서는 안 보인다. 줄을 누르면 수정 시트가 열린다
- 화면 안에 "+ 기록" — 그래프를 보다 재보는 흐름이라, 홈으로 돌아가지 않게 했다
- 측정이 없으면 안내, 한 번뿐이면 수치만 내고 그래프를 그리지 않는다

하단 탭에 없는 하위 화면이라 전체 기록보기와 같은 방식으로 붙였다(tab 값으로
열고 헤더의 뒤로가기로 통계로 돌아간다). 화면 수가 더 늘면 라우터를 검토한다.

그래프에서 몸무게가 내내 같으면 값의 폭이 0이라 눈금이 한 줄에 겹친다 —
그럴 때만 ±0.5kg을 임의로 벌린다.

검증: 좌표를 직접 재서 확인했다 — x축 날짜 비례(2일:29일 간격 = 16.9px:245.1px,
비율 14.5로 실제 날짜 비율과 일치), 눈금 5줄 40px 등간격, kg 라벨이 각 눈금선과
정렬, 점 좌표가 값과 일치. 화면 동작도 실기기에서 확인했다.

## v0.10 마이페이지 (2026-08-03)

**유실분 복구가 끝났다.** 7/24~7/30 작업분 중 마지막으로 남아 있던 화면이다.

- 계정 카드: 프로필 사진·이름·이메일 + 로그아웃. 이메일이 없으면 provider
  이름으로 대신한다(카카오는 이메일 동의를 안 한 계정이 있을 수 있다)
- 반려동물 관리: 목록·전환·수정·추가. 지금 보고 있는 펫에 "보는 중" 배지를
  단다 — 그 선택이 홈·기록·통계 전부의 기준이라 여기서도 드러나야 한다.
  등록 폼은 `PetForm` 하나를 그대로 쓴다
- 설정: 푸시 알림 "준비 중" 표기. 일정마다 notificationSetting은 저장되지만
  예약은 미구현이라(09_Todo 우선순위 5), 켤 수 있는 것처럼 두면 알림을
  기다리게 된다
- 앱 정보: 버전. `app.json`을 그대로 읽는다 — 두 군데 적어 두면 어긋난다

최대 5마리 상한(02 §2)이 펫 전환 메뉴와 이 화면 두 곳에 필요해져
`MAX_PETS`를 `src/pets.js`로 옮겼다.

이로써 09_Todo "재구현이 필요한 것"이 비었다. 앞으로는 재구현이 아니라 새
기능이다 — MedicalRecord 연결, 기록 먼저 생성 시 안내, 푸시 알림, 회원 탈퇴,
기록 목록 페이지네이션.

## v0.11 일정과 실제 기록 연결 (2026-08-03)

03_DB_Design "일정과 기록의 관계"의 두 흐름을 붙였다. 재구현이 아니라 유실 전에도
없던 새 기능이다.

**일정 완료 → 완료 기록.** 완료를 누르면 시행일·병원·제품·메모를 확인받는 시트가
뜨고, 확인하면 `medical_records` 행을 만들어 `linked_record_id`로 잇는다.
"일정 완료 시" 네 단계(① 기록 생성 ② 상태 완료 ③ 연결 ④ 반복이면 다음 일정)가
모두 돌아간다.

계획값을 그냥 복사하지 않고 한 번 묻는다. 계획과 실제가 어긋나는 것이 완료
기록을 일정과 따로 두는 이유 자체다 — 며칠 늦게 갔거나 다른 병원에 갔거나.
특히 **시행일 기본값은 예정일이 아니라 오늘**이다. 지난 일정을 뒤늦게 체크하는
경우가 많아 예정일을 그대로 쓰면 실제로 간 날과 어긋난다.

탭이 하나 늘었지만 실행취소는 그대로다. 되돌릴 것이 셋으로 늘어(상태, 완료 기록,
자동 생성된 다음 일정) 셋을 한꺼번에 되돌린다. 하나만 되돌리면 어긋난 상태가
남는다.

**기록 먼저 → 다음 일정 안내.** 더보기 → 병원 기록으로 일정 없이 다녀온 것을
남길 수 있다. 저장하면 "다음 일정도 등록할까요"가 뜨고, 등록을 고르면 종류·
병원·제품이 채워진 일정 폼이 열린다(06_UserFlow "기록 먼저 생성"). 일정을
완료한 경우에는 묻지 않는다 — 반복 주기가 이미 정한 일을 두 번 만들게 된다.

완료 기록은 일정 탭 아래 "완료 기록" 섹션에서 본다. 전체 기록보기는
`health_records` 기준이라 다른 테이블을 섞을 자리가 아니다.

곁다리로 종류·라벨 같은 표현 상수를 `src/schedules.js`로 옮겼다. scheduleRepo가
완료 기록을 만들려면 medicalRepo를 부르고 medicalRepo는 라벨이 필요해서, 표현이
repo 안에 있으면 순환 참조가 된다.

검증: 웹 번들 빌드 통과. 실기기 확인이 남아 있다 — 특히
`medical_records.medical_type`에 서버 CHECK 제약이 있는지가 미확인이다
(09_Todo "아직 확인 못 한 것").
